import { reactive, readonly } from "vue";

const translatestates = reactive({
  source: "",
  byFirst: "",
  bySecond: "",
  byMe: "",
});

const festivalStates = reactive({});

export default { translatestates, festivalStates };
